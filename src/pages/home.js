
import React from 'react'
import { Typography, Button } from '@mui/material'

const home = () => {
    return (
        <div>
            <Typography variant='h1'> The Blogs</Typography>
            <Button variant="contained">Contained</Button>
        </div>
    )
}

export default home
